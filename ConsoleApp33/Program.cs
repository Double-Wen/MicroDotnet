using System;

namespace ConsoleApp33
{
    class Program
    {
        static void Main(string[] args)
        {
            int CPlus = 89;
            int CSharp = 90;
            int SQL = 60;
            int sub = CSharp - SQL;
            Console.WriteLine(sub);
            double avg = (CPlus + CSharp + SQL) / 3;
            Console.WriteLine(avg);
        }
    }
}
